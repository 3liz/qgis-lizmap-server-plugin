name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Execute dry run
        required: true
        type: boolean
        default: false

jobs:
  release-dry-run:
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == '3liz' && inputs.dry_run == true}}
    steps:
      - uses: actions/checkout@v6

      - name: Fetch yapt
        run: |
          curl ${{ vars.YAPT_REPOSITORY_URL }} | tar -xzf -
      
      - name: Fetch current changelog
        run: |
          ./yapt-pkg changelog >> release.md
          cat release.md

      - name: Package and deploy the zip
        run: >
          ./yapt-pkg -v package --publish --dry-run
          --osgeo-username "${{ secrets.OSGEO_USERNAME }}"
          --osgeo-password "${{ secrets.OSGEO_PASSWORD }}"

  release:
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == '3liz' && contains(github.ref, 'refs/tags/') && inputs.dry_run == false }}

    steps:
      - uses: actions/checkout@v6
      
      - name: Fetch yapt
        run: curl ${{ vars.YAPT_REPOSITORY_URL }} | tar -czf -
      
      - name: Fetch current changelog
        run: |
          ./yapt-pkg changelog >> release.md
          cat release.md

      - name: Package and publish
        run: >
          ./yapt-pkg -v package --publish 
          --osgeo-username "${{ secrets.OSGEO_USERNAME }}"
          --osgeo-password "${{ secrets.OSGEO_PASSWORD }}"

      - name: Create release on GitHub
        uses: ncipollo/release-action@v1.20
        with:
          bodyFile: release.md
          artifacts: "*.zip"
          token: ${{ secrets.BOT_HUB_TOKEN }}

